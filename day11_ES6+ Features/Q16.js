// Arrow Function 
const isEven = n => n % 2 === 0;

// Ternary Operator Rewrite
const result = marks >= 35 ? "Pass" : "Fail";

// Arrow Function with Ternary for Default Value
const greet = name => `Hello, ${name ? name : "Guest"}`;
